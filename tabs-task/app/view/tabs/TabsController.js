Ext.define('TabsTask.view.tabs.TabsController', {
	extend: 'Ext.app.ViewController',
	alias: 'controller.tabscontroller'
	/*
    onTabChange: function(tabpanel, tab) {
    switch(tab.id) {
        case 'firstTab':
            // check the color in the combobox
            break;
        case 'secondTab':
            // check the color in the combobox and apply it on the background color of the button
            break;
        case 'thirdTab':
            // check the html content in the first tab and apply the content here
            break;
    	}
    },
    onSelectedColor: function() {
        var getColor = this.lookupReference('color').getValue('selectedColor');
        return this.lookupReference('color').setValue('selectedColor');
    },	
	
	// METHODS THAT MIGHT COME USEFUL

    setBind( bind )
	Sets the value of bind.

	componentCls : String
	CSS Class to be added to a components root level element to give distinction to it via styling.

	*/
});